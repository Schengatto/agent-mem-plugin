# MemoryMesh API — runtime dependencies (Sprint 1 — F1-04 + F1-05 + F1-09)
#
# Pin semantico human-maintained. requirements-hashes.txt (generato da
# pip-compile --generate-hashes) sarà introdotto in F2-01 insieme al wiring
# FastAPI completo. L'insieme completo (FastAPI, httpx, asyncpg, ecc.)
# arriva con F2-01.
#
# Vedi docs/DEPENDENCIES.md per ADR versioni.

# ─── DB & migrations (F1-04) ─────────────────────────────────────────────
sqlalchemy==2.0.*           # metadata + MigrationContext di Alembic
alembic==1.14.*             # schema migrations
psycopg[binary]==3.2.*      # sync driver usato da Alembic (asyncpg arriva in F2-01)

# ─── Redis Streams (F1-05) ───────────────────────────────────────────────
redis==5.2.*                # client Python per Redis 8 (async/asyncio)

# ─── mDNS broadcaster (F1-09) ────────────────────────────────────────────
zeroconf==0.135.*           # _memorymesh._tcp.local broadcaster (LAN discovery)

# ─── Validation / config ─────────────────────────────────────────────────
pydantic==2.13.*            # config.py + schemi EmbedJob/DistillJob/TightenJob/DlqEnvelope
pydantic-settings==2.7.*    # env loader
